# S3 specific settings
bucket_name = "<bucket name>"

# Ubuntu Image for the registry, and a key pair
ami_id = "<ami id for an ubuntu cloud image>"
key_pair = "<pre-created key pair name>"

# General settings
access_key = "<Access Key>"
secret_key = "<Secret Key>"
symphony_ip = "<Symphony IP>"
